#: import DICT_TRANSDUCER cst.DICT_TRANSDUCER

<MainWindow>:
	treeDisplay: td
	treeLabel: texte
	orientation: "horizontal"
	RelativeLayout:
		TreeDisplay:
			id: td
			nodeClick: texte
			pos: (self.parent.width-self.width)/2, (self.parent.height-self.height)/2
			size_hint: None, None
			size: 0.7 * 800, 0.7 * 600
			on_treeChange: texte.updateTree()
			
		# DEBUG LABEL
		# Label:
		# 	pos: (0, 0)
		# 	text: str(td.ctrlHeld)
		
	BoxLayout:
		orientation: 'vertical'
		size_hint_x: 0.7
		BoxLayout:
			size_hint_y: 0.1
			orientation: 'horizontal'
			Spinner:
				text: "QTree"
				values: DICT_TRANSDUCER.keys()
				on_text: texte.changeTransducer(args[1])
				size_hint_x: 0.7
			Button: # CLEAR BUTTON
				text: "C"
				on_press: td.clearLabels()
				size_hint_x: 0.15
			Button: # CLEAR BUTTON
				size_hint_x: 0.15
				on_press: root.importTree.createPopUp()
				text: "I"
		TreeInput:
			id: texte
			multiline: False
			on_text: td.displayChange = not td.displayChange
			on_text_validate: self.copy(self.text)




